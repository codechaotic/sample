mongo:
  image: mongo
  command: mongod --smallfiles
app:
  image: code/node
  working_dir: /app
  restart: on-failure:5
  ports:
    - 127.0.0.1:80:8080
  command: npm start
  volumes:
    - build/dist/:/app
  links:
    - mongo
